# ── Core GUI + 3D ────────────────────────────────────────────────
PyQt6>=6.4.0
pyqtgraph>=0.13.0
PyOpenGL>=3.1.0
numpy>=1.24.0

# ── Robot: G-code over serial (COM port) ─────────────────────────
pyserial>=3.5

# ── VIVE Tracker via SteamVR / OpenVR ────────────────────────────
openvr>=1.26.0

# ── Optional ─────────────────────────────────────────────────────
# FAIRINO robot control over TCP/IP. Not on PyPI — install the SDK that ships
# with your FAIRINO controller / FAIRINO_SimMachine on the robot laptop:
#   fairino
#
# Dev-only: regenerate the bundled VIVE Tracker mesh from the official STEP CAD
# (tools/convert_step_to_stl.py). NOT needed at runtime — the meshed STL is
# already bundled in robotplay/assets/.
#   gmsh
#
# Packaging to a standalone .exe:
# pyinstaller>=5.0
